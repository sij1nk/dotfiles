# alt key
set $mod Mod4

set $left h
set $down j
set $up k
set $right l

# Drag floating windows by holding down $mod and left mouse button.
# Resize them with right mouse button + $mod.
# Despite the name, also works for non-floating windows.
# Change normal to inverse to use left mouse button for resizing and right
# mouse button for dragging.
floating_modifier $mod normal

bindsym {

# Kill focused window
    $mod+q kill

# Reload the configuration file
    $mod+Shift+c reload

# Exit sway (logs you out of your Wayland session)
    $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'

# Move your focus around
    $mod+$left focus left
    $mod+$down focus down
    $mod+$up focus up
    $mod+$right focus right

# TODO: fix alt tab (currently cycles in layout instead of workspace)
# https://github.com/yoshimoto/i3-alt-tab.py
    mod1+Tab focus next
    mod1+Shift+Tab focus prev

    $mod+control+$left move left
    $mod+control+$down move down
    $mod+control+$up move up
    $mod+control+$right move right

# Switch to workspace
    $mod+1 workspace number 1
    $mod+2 workspace number 2
    $mod+3 workspace number 3
    $mod+4 workspace number 4
    $mod+5 workspace number 5
    $mod+6 workspace number 6
    $mod+7 workspace number 7
    $mod+8 workspace number 8
    $mod+9 workspace number 9
    $mod+0 workspace number 10
# Move focused container to workspace
    $mod+Shift+1 move container to workspace number 1
    $mod+Shift+2 move container to workspace number 2
    $mod+Shift+3 move container to workspace number 3
    $mod+Shift+4 move container to workspace number 4
    $mod+Shift+5 move container to workspace number 5
    $mod+Shift+6 move container to workspace number 6
    $mod+Shift+7 move container to workspace number 7
    $mod+Shift+8 move container to workspace number 8
    $mod+Shift+9 move container to workspace number 9
    $mod+Shift+0 move container to workspace number 10

    $mod+b splith
    $mod+v splitv

# Switch the current container between different layout styles
# NOTE: commented, because unused (aside from mistaken presses)
    # $mod+control+s layout stacking
    # $mod+control+t layout tabbed
    # $mod+control+e layout toggle split

    $mod+o workspace back_and_forth

# Make the current focus fullscreen
    $mod+f fullscreen

    $mod+control+s exec "screenshot"
    $mod+control+shift+s exec "screenshot full"

# Toggle the current focus between tiling and floating mode
    $mod+Shift+space floating toggle
# Swap focus between the tiling area and the floating area
    $mod+space focus mode_toggle

    $mod+i focus parent
    $mod+shift+i focus child

# TODO: scratchpad stuff; figure out what to use it for (how to list windows in scratchpad?)
# Move the currently focused window to the scratchpad
    $mod+Shift+minus move scratchpad
# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
    $mod+minus scratchpad show

# TODO: this is bad; should resize in the direction we're pressing
    $mod+shift+$left resize shrink width 10px
    $mod+shift+$down resize grow height 10px
    $mod+shift+$up resize shrink height 10px
    $mod+shift+$right resize grow width 10px

    $mod+apostrophe exec brightness up
    $mod+semicolon exec brightness down

    $mod+bracketleft exec pulsemixer --change-volume -5
    $mod+bracketright exec pulsemixer --change-volume +5
    $mod+ctrl+m exec pulsemixer --toggle-mute

    $mod+ctrl+p exec confset playerctl toggle
    $mod+ctrl+shift+p exec confset playerctl select-player
    $mod+ctrl+bracketleft exec confset playerctl prev
    $mod+ctrl+bracketright exec confset playerctl next
    $mod+ctrl+shift+bracketleft exec confset playerctl skip -- -10
    $mod+ctrl+shift+bracketright exec confset playerctl skip 10

    $mod+a mode "command_layer"

# applications:
# TODO: email / calendar application
# TODO: terminal flags are not interchangeable between different terminals, so if switching from
# alacritty to something else, the commands utilizing them will break
    $mod+Return exec $term
    $mod+p exec $menu
}

mode "command_layer" {
{{#each apps}}
    bindsym $mod+{{key}} exec focus_app {{binary}} {{workspace}} {{selector}}
{{/each}}
    bindsym 3 exec swaymsg mode "default" && plsdo game
    bindsym 2 exec swaymsg mode "default" && ytdlmenu $(wl-paste) > /tmp/y.txt 2>&1
    bindsym 1 exec swaymsg mode "default" && plsdo keyboard_layout
    bindsym 0 exec swaymsg mode "default" && plsdo power
    bindsym Escape mode "default"
}
